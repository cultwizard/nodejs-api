# Node.js API - Test Case 3

This repository contains the implementation for Test Case 3: Node.js Application Setup & Git Integration.

## Features
- A simple Express server setup in `index.js`.
- Configured to listen on port `8080`.
- Includes a `/health` endpoint that returns HTTP status 200 with the JSON response `{ "status": "OK" }`.
